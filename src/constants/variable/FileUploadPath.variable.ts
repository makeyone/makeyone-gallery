import { NODE_ENV } from '@/constants/environment';

export const postImageUploadPath = `uploads/${NODE_ENV}/posts/images/main_and_list`;
export const postContentImageUploadPath = `uploads/${NODE_ENV}/posts/images/content`;
export const profileImageUploadPath = `uploads/${NODE_ENV}/profile/images/avatar`;
